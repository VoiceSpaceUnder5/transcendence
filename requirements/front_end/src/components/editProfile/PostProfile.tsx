import React from 'react';

interface ChangedProfile {
  name: string;
  id: number;
  email: string;
  description: string;
  imagePath: string;
}

// 바뀐 state 다 받아서 post 보내기.
// setMyProfile

export default function PostProfile(props: ChangedProfile) {
  return <></>;
}
